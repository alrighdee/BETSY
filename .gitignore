# Android / Gradle
app/build/
app/.gradle/
app/local.properties
.gradle/
build/

# Python
tools/__pycache__/
*.pyc

# Local tooling / editor
.DS_Store

# Release signing, NEVER commit. Losing these means users must uninstall to update.
betsy-release.jks
keystore.properties

# Local scratch, not part of the project
scratch/

# Live probe scratch: the *.console files are duplicates of the *.log written by the
# probe tools, and harvest-live/svc12-sweep are fixed names that get overwritten on each
# run rather than timestamped. The timestamped captures are the record worth keeping.
captures/*.console

# Internal agent instructions. Local only: not part of the published project.
AGENTS.md
